import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

const PositionSettings = () => {
  return (
    <Card className="bg-fitness-card">
      <CardHeader>
        <CardTitle className="text-fitness-text">Position Settings</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Position settings content will go here */}
      </CardContent>
    </Card>
  )
}

export default PositionSettings